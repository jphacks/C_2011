<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>ManagerAssistant SNS</title>
        <script src="/send_url.js"></script>
    </head>

    <body>
        <header>
            <a href="/">Manager Assistant</a>
        </header>

        <div id="table">
            <div id="table-title">
                <div>アカウント管理</div>
                <a href="">修正する</a>
                <% console.log("アカウント情報を修正するとこ決まってない") %>
            </div>

            <div id="table-wrapper">
                <ul class="table-body">
                    <% item = {id : 1, name : "aaa"} %>
                    <% console.log("sns_list.ejs データベース決まってない") %>
                    <li>
                        <div>
                            <span>id</span>
                            <span><%= item.id %></span>
                        </div>
                        <div>
                            <span>name</span>
                            <span><%= item.name %></span>
                        </div>
                    </li>
                </ul>
            </div>

        </div>

    </body>
</html>