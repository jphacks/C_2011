<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><!--システム名-->aaa_record</title>
        <script src="/send_url.js"></script>
    </head>

    <body>
        <header>
            <a href="/"><!--システム名-->aaa</a>
            <% console.log("システム名決まってないよーーーーーーーーー") %>
        </header>

        <div id="table">
            <div id="table-title">
                <div>タイム履歴</div>
            </div>

            <div id="table-wrapper">
                <div class="table-head">
                    <span>日にち</span>
                    <span>時間</span>
                    <span>距離</span>
                    <span>タイム</span>
                </div>

                <ul class="table-body">
                    <% items = [{day : "2020-11-02", time : "09:50:32", distance : 50, record : "5.93"},
                                {day : "2020-11-02", time : "09:53:41", distance : 100, record : "10.81"}] %>
                    <% console.log("データベース決まってない　できれば日にちが新しい順に出力") %>
                    <% items.forEach((item) => { %>
                        <li>
                            <span><%= item.day %></span>
                            <span><%= item.time %></span>
                            <span><%= item.distance %></span>
                            <span><%= item.record %></span>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </div>
    </body>
</html>